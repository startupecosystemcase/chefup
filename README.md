# ChefUp

Веб-приложение для нетворкинга и поиска работы в сфере HoReCa.

## 🚀 Быстрый старт

Приложение уже запущено на **http://localhost:3000**

## 📋 Технологии

- **Next.js 14+** (App Router)
- **TypeScript** (строгая типизация)
- **Tailwind CSS** (стилизация)
- **shadcn/ui** (UI компоненты)
- **Zustand** (управление состоянием)
- **React Hook Form + Zod** (формы и валидация)
- **Lucide React** (иконки)
- **react-hot-toast** (уведомления)

## 📁 Структура проекта

```
chefup/
├── app/                    # Next.js App Router
│   ├── (auth)/            # Группа маршрутов для авторизации
│   ├── (protected)/       # Защищенные маршруты
│   ├── profile/           # Публичные профили
│   ├── layout.tsx         # Корневой layout
│   ├── page.tsx           # Главная страница
│   └── globals.css        # Глобальные стили
├── components/            # React компоненты
│   ├── ui/               # shadcn/ui компоненты
│   ├── Navbar.tsx        # Навигация
│   ├── Footer.tsx        # Футер
│   ├── BadgeSelector.tsx # Компонент выбора badges
│   └── OnboardingWizard.tsx # Многошаговая анкета
├── lib/                   # Утилиты и данные
│   ├── utils.ts          # Вспомогательные функции
│   └── data.ts           # Константы данных
├── stores/                # Zustand stores
│   └── useOnboardingStore.ts
└── types/                 # TypeScript типы
    └── onboarding.types.ts
```

## 🎯 Основные функции

1. **Лендинг** (`/`) - маркетинговая страница
2. **Регистрация** (`/auth`) - регистрация по номеру телефона
3. **Анкета** (`/onboarding`) - многошаговая форма заполнения профиля
4. **Личный кабинет** (`/dashboard`) - просмотр и редактирование профиля
5. **Публичный профиль** (`/profile/[id]`) - просмотр профиля пользователя

## 🛠 Команды

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Запуск production сервера
npm start

# Линтинг
npm run lint
```

## 📝 Особенности

- ✅ Полная типизация TypeScript
- ✅ Валидация форм с Zod
- ✅ Сохранение данных в localStorage (Zustand persist)
- ✅ Адаптивный дизайн (mobile-first)
- ✅ Геймификация (progress bar, badges)
- ✅ Защита маршрутов
- ✅ Маска ввода телефона
- ✅ Многошаговая форма с валидацией

## 🎨 Дизайн

- Основной цвет: `#F97316` (оранжевый)
- Шрифт: Inter (поддержка латиницы и кириллицы)
- Адаптивность: Mobile-first подход

## 📱 Маршруты

- `/` - Главная страница
- `/auth` - Регистрация
- `/onboarding` - Заполнение анкеты (защищенный)
- `/dashboard` - Личный кабинет (защищенный)
- `/profile/[id]` - Публичный профиль

## 🔐 Аутентификация

В текущей версии используется mock-аутентификация:
- При регистрации генерируется UUID
- Данные сохраняются в localStorage через Zustand persist
- Защищенные маршруты проверяют наличие userId

## 📦 Данные анкеты

Анкета включает:
- Личную информацию (имя, фамилия, город, возраст)
- Опыт работы (позиция, опыт, образование, разряд)
- Навыки (сертификаты, кухни, дополнительные навыки)
- Предпочтения по работе (формат заведения, зарплата)
- Цели и контакты

## 🚀 Деплой на Netlify

Проект настроен для деплоя на Netlify:

1. **Создайте репозиторий на GitHub:**
   ```bash
   # Создайте новый репозиторий на GitHub (через веб-интерфейс)
   # Затем выполните:
   git remote add origin https://github.com/YOUR_USERNAME/chefup.git
   git branch -M main
   git push -u origin main
   ```

2. **Подключите к Netlify:**
   - Зайдите на [netlify.com](https://netlify.com)
   - Нажмите "Add new site" → "Import an existing project"
   - Выберите GitHub и авторизуйтесь
   - Выберите репозиторий `chefup`
   - Настройки сборки:
     - **Build command:** `npm run build`
     - **Publish directory:** `.next`
   - Нажмите "Deploy site"

3. **Настройки окружения (если нужны):**
   - Site settings → Environment variables
   - Добавьте необходимые переменные окружения

Файл `netlify.toml` уже настроен для автоматического деплоя.

---

**Приложение готово к использованию!** 🎉

